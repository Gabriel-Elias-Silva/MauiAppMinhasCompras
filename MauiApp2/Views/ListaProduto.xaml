<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MauiApp2.ViewModels"
             x:Class="MauiApp2.Views.ListaProduto"
             Title="Lista de Produtos">

    <ContentPage.BindingContext>
        <vm:ProdutosViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Novo" Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>

    <StackLayout Padding="10" Spacing="10">
        <SearchBar Placeholder="Pesquisar produtos"
                   TextChanged="OnSearchBarTextChanged"/>

        <CollectionView ItemsSource="{Binding Produtos}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal" Padding="5">
                        <Label Text="{Binding Descricao}" WidthRequest="120"/>
                        <Label Text="{Binding Quantidade}" WidthRequest="60"/>
                        <Label Text="{Binding Preco}" WidthRequest="60"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>
